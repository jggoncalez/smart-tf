
import { localMode } from "./modules/localMode"

function main() {
    if (server) {
        if (sensor) {

        }
        else {
            securityMode()
        }

    } else {
        localMode()
    }

    
    updateTrafficLight(currentColor);
}